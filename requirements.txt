# SC Labs - Supply Chain Optimization
# Python >= 3.10 required
#
# Core dependencies for supply chain optimization modules

# Data manipulation and analysis
pandas>=1.5.0,<3.0.0
numpy>=1.23.0,<2.0.0

# For configuration file parsing
yaml

# Time series forecasting with LightGBM
lightgbm>=4.0.0

# Statistical functions for inventory optimization
scipy>=1.9.0,<2.0.0

# Authentication and database (optional - for production)
supabase>=2.0.0       # Supabase client for authentication
bcrypt>=4.0.0         # Password hashing for authentication
python-dotenv>=1.0.0  # Load environment variables from .env

#Streamlit for web app (needed during deployment)
streamlit>=1.20.0
